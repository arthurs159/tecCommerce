import './styles.css';

const Button = () => {
  return (
    <div className="button-container">
      <div className="button-div">
        <button type="button" className="bg-primary">
          <h4>Mostrar catálogo</h4>
        </button>
      </div>
    </div>
  );
};

export default Button;
